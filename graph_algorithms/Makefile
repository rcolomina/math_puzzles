# graph_algorithms — tree / stack-machine puzzles.
# word_machine.cpp is a work-in-progress and does not compile yet, so it is
# excluded from the build (see docs for details).
EXCLUDE := word_machine.cpp
include ../common/topic.mk
